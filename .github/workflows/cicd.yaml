name: CICD

on:
  push:
    tags-ignore: [v*.*.*]
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo TODO
# check formatting
# check linting
# build contracts

# run unit/fuzz tests
# check code coverage
# run invariant tests

# generate code coverage report
# generate gas report
# generate static analysis report
# generate docs from natspec

# deploy to testnet (PoS)
# verify on Etherscan
# run smoke tests

# package sources, artifacts, reports, and documentation
# tag with incremented version
# publish package on npm
